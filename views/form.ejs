<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Image Full Card</title>
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
    />
    <!-- style -->
    <link rel="stylesheet" href="style.css" />
    <!-- font -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap"
        rel="stylesheet"
    />
    <script type="text/javascript"
        src="https://app.sandbox.midtrans.com/snap/snap.js"
        data-client-key="SB-Mid-client-jURaIXO0d2-1fKos"></script>
</head>
<body>
    <section class="order-hero d-flex justify-content-center align-items-center min-vh-100">
        <div class="container mx-3 my-3 container-order rounded-4">
            <div class="row">
                <!-- Detail Event -->
                <div class="col-md-6">
                    <div class="main-content">
                        <div class="container-image pt-2">
                            <img class="justify-content-center mx-3 my-3" src="img/jakartafair.png" alt="jakartafair" />
                        </div>
                        <div class="text-content justify-content-center mx-3">
                            <h1 class="fw-bold">Jakarta Fair</h1>
                            <p class="fs-6">
                                Jakarta Fair, or Jakarta International Expo (JIExpo), is Indonesia's largest annual event,
                                held from June to July. It features a wide range of products from various industries,
                                including automotive, electronics, and fashion. The fair offers entertainment, cultural
                                performances, concerts, and amusement rides, making it a vibrant celebration for both
                                locals and tourists. It promotes local businesses and showcases Indonesia's rich diversity.
                            </p>
                        </div>
                    </div>
                </div>
                <!-- Add Ticket -->
                <div class="col-md-6">
                    <p class="my-3 mx-3 text-light">Book Ticket Here:</p>
                    <div class="order-content mb-3 mx-3">
                        <div class="order-box d-flex rounded-4 mb-3" style="background: white">
                            <div class="ticket1">
                                <h4>Reguler Ticket</h4>
                                <p style="margin: 0">Rp 120,000</p>
                            </div>
                            <div class="button-order justify-content-center align-items-center my-3">
                                <div class="ticket-input">
                                    <button type="button" id="decrease1" class="ticket-button">-</button>
                                    <span id="ticket-count1">1</span>
                                    <button type="button" id="increase1" class="ticket-button">+</button>
                                </div>
                            </div>
                        </div>
                        <div class="order-box d-flex rounded-4 mb-3" style="background: white">
                            <div class="ticket1">
                                <h4>VIP Ticket</h4>
                                <p style="margin: 0">Rp 180,000</p>
                            </div>
                            <div class="button-order justify-content-center align-items-center my-3">
                                <div class="ticket-input">
                                    <button type="button" id="decrease2" class="ticket-button">-</button>
                                    <span id="ticket-count2">1</span>
                                    <button type="button" id="increase2" class="ticket-button">+</button>
                                </div>
                            </div>
                        </div>
                        <div class="order-box d-flex rounded-4 mb-3" style="background: white">
                            <div class="ticket1">
                                <h4>VVIP Ticket</h4>
                                <p style="margin: 0">Rp 220,000</p>
                            </div>
                            <div class="button-order justify-content-center align-items-center my-3">
                                <div class="ticket-input">
                                    <button type="button" id="decrease3" class="ticket-button">-</button>
                                    <span id="ticket-count3">1</span>
                                    <button type="button" id="increase3" class="ticket-button">+</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="container summary-container">
                <div class="row mt-3 rounded-4 row-sum-order">
                    <div class="col summary-box">
                        <h3 style="margin: 0">Order Summary:</h3>
                    </div>
                    <div class="col summary-box">
                        <p style="margin: 0">Reguler Ticket:</p>
                        <p style="margin: 0" id="summary-regular">Rp 120,000</p>
                    </div>
                    <div class="col summary-box">
                        <p style="margin: 0">Fees:</p>
                        <p style="margin: 0" id="summary-fees">Rp 20,000</p>
                    </div>
                    <div class="col summary-box">
                        <p style="margin: 0">Total:</p>
                        <p style="margin: 0" id="summary-total">Rp 140,000</p>
                    </div>
                    <div class="col col-lg-2 summary-box d-flex align-items-center justify-content-center">
                        <button id="checkoutBtn">
                            <p style="margin: 0">Check Out</p>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <script>
        // Calculate the total amount
        function calculateTotal() {
            const regularPrice = 120000;
            const vipPrice = 180000;
            const vvipPrice = 220000;
            const fees = 20000;
    
            const regularTicket = parseInt(document.getElementById('ticket-count1').textContent);
            const vipTicket = parseInt(document.getElementById('ticket-count2').textContent);
            const vvipTicket = parseInt(document.getElementById('ticket-count3').textContent);
    
            const total = (regularTicket * regularPrice) + (vipTicket * vipPrice) + (vvipTicket * vvipPrice) + fees;
            document.getElementById('summary-total').textContent = `Rp ${total.toLocaleString()}`;
        }
    
        // Event listeners for input changes
        document.getElementById('increase1').addEventListener('click', function() {
            let count = parseInt(document.getElementById('ticket-count1').textContent);
            document.getElementById('ticket-count1').textContent = ++count;
            calculateTotal();
        });
        document.getElementById('decrease1').addEventListener('click', function() {
            let count = parseInt(document.getElementById('ticket-count1').textContent);
            if (count > 0) {
                document.getElementById('ticket-count1').textContent = --count;
                calculateTotal();
            }
        });
        document.getElementById('increase2').addEventListener('click', function() {
            let count = parseInt(document.getElementById('ticket-count2').textContent);
            document.getElementById('ticket-count2').textContent = ++count;
            calculateTotal();
        });
        document.getElementById('decrease2').addEventListener('click', function() {
            let count = parseInt(document.getElementById('ticket-count2').textContent);
            if (count > 0) {
                document.getElementById('ticket-count2').textContent = --count;
                calculateTotal();
            }
        });
        document.getElementById('increase3').addEventListener('click', function() {
            let count = parseInt(document.getElementById('ticket-count3').textContent);
            document.getElementById('ticket-count3').textContent = ++count;
            calculateTotal();
        });
        document.getElementById('decrease3').addEventListener('click', function() {
            let count = parseInt(document.getElementById('ticket-count3').textContent);
            if (count > 0) {
                document.getElementById('ticket-count3').textContent = --count;
                calculateTotal();
            }
        });
    
        // Handle checkout button click
        document.getElementById('checkoutBtn').addEventListener('click', () => {
            const regularTicket = parseInt(document.getElementById('ticket-count1').textContent);
            const vipTicket = parseInt(document.getElementById('ticket-count2').textContent);
            const vvipTicket = parseInt(document.getElementById('ticket-count3').textContent);
            const fees = 20000;
            const grossAmount = parseInt(document.getElementById('summary-total').textContent.replace(/Rp\s|,/g, ''));
    
            const payload = {
                Regular_Ticket: regularTicket,
                VIP_Ticket: vipTicket,
                VVIP_Ticket: vvipTicket,
                Fees: fees,
                gross_amount: grossAmount
            };
    
            fetch('http://localhost:5000/pay', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(payload)
            })
            .then(response => response.json())
            .then(data => {
                if (data.transactionToken) {
                    // Directly show the payment interface without redirecting
                    window.snap.pay(data.transactionToken, {
                        onSuccess: function(result) {
                            alert("Payment successful");
                            console.log(result);
                        },
                        onPending: function(result) {
                            alert("Waiting for your payment!");
                            console.log(result);
                        },
                        onError: function(result) {
                            alert("Payment failed!");
                            console.log(result);
                        },
                        onClose: function() {
                            alert('You closed the popup without finishing the payment');
                        }
                    });
                } else {
                    alert('Payment failed');
                }
            })
            .catch(error => {
                console.error('Error:', error);
            });
        });
    
        // Initial calculation of the total
        calculateTotal();
    </script>
    
</body>
</html>
